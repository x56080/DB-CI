<!--@param ${basic.testcase.path}: The path where test case direcotry.-->
<!--@param ${install.path}: The path where sequoiadb installed.-->
<!--@param ${antTask.jar.path}: The antTask.jar's path.-->
<!--@param ${reports.path}: The path where test case direcotry.-->
<!--@param ${test.package.name}: The name of test package name,.-->
<!--@param ${parallel.num}:  The parallel number when parallel in same host.-->
<!--@param ${excludes.dir.patterns}: A comma- or space-sparated list of patterns of directories that must be excluded.-->
<!--@param ${ci.properties.path}: where is the ci.properties file -->
<!--@param ${report.suffix}:this is for long time test, default is NULL-->
<project name="test-basic-testcases" default="Test-Basic-Testcases">
	<property file="${ci.properties.path}" />
	<property name="sdb.fullname" value="${install.path}/sequoiadb/bin/sdb" />
	<property name="to.copy.test.log" value="true"/>
	<taskdef resource="com/sequoiadb/ant/ant-sdbtasks.xml">
		<classpath>
			<fileset dir="${antTask.jar.path}" includes="*.jar" />
		</classpath>
	</taskdef>

	<target name="Clean-Basic-Reports" depends="">
		<echo message="Reports' path is ${reports.path}" />
		<delete dir="${basic.testcase.path}/reports${parallel.num}" failonerror="false" />
		<mkdir dir="${reports.path}" />
		<echo message="Clean deploy result reports compeleted." />
	</target>

	<target name="Test-Basic-Testcases" depends="Clean-Basic-Reports">
		<echo message="Begin test basic testcases..." />
		<echo message="Perform the basic testcases,with testcase.path=${basic.testcase.path}" />

		<property name="sdb.cmd.allPrepare" value=" -e &quot;var COORDSVCNAME=${coord.service.port};&quot; -f &quot;${basic.testcase.path}/libs/func.js,${basic.testcase.path}/libs/all_prepare.js&quot;" />
		<echo message="Exec:${sdb.fullname} ${sdb.cmd.allPrepare}" />
		<exec executable="${sdb.fullname}" dir="" timeout="300000" outputproperty="allPrepare.screen.output"  resultproperty="allPrepare.ret.code">
			<arg line="${sdb.cmd.allPrepare}" />
		</exec>
		<echo message="${allPrepare.screen.output}" />

		<foreach target="test-one-suite" param="test.suite.fullname" >
			<path>
				<dirset dir="${basic.testcase.path}/js" includes="${includes.dir.patterns}" excludes="${excludes.dir.patterns}" />
			</path>
			<param name="testcases.path" value="${basic.testcase.path}" />
			<param name="test-type" value="basic" />
			<param name="includes.case.patterns" value="*.js" />
			<param name="excludes.case.patterns" value="commlib.js" />
		</foreach>

		<property name="sdb.cmd.allClean" value=" -e &quot;var COORDSVCNAME=${coord.service.port};&quot; -f &quot;${basic.testcase.path}/libs/func.js,${basic.testcase.path}/libs/all_clean.js&quot;" />
		<echo message="Exec:${sdb.fullname} ${sdb.cmd.allClean}" />
		<exec executable="${sdb.fullname}" dir="" timeout="300000" outputproperty="allClean.screen.output" resultproperty="allClean.ret.code">
			<arg line="${sdb.cmd.allClean}" />
		</exec>
		<echo message="${allClean.screen.output}" />
	</target>

	<target name="test-one-suite" depends="">
		<hostinfo prefix="host" />
		<basename property="test.suite.name" file="${test.suite.fullname}" />
		<!-- <basename property="deploy.name" file="${deploy.case.fullname}" suffix=".xml" />
		<property name="test.suite.class.name" value="testcases-${test-type}-${deploy.name}-${host.NAME}-${parallel.num}-${test.suite.name}" /> -->
		<property name="test.suite.class.name" value="com.sequoiadb.testcases-js.${test.package.name}.${host.NAME}-${parallel.num}-${test.suite.name}" />

		<echo message="###test.package.name=${test.package.name}###" />

		<echo message="Now testsuite ${test.suite.name} ..." />
		<echo file="${reports.path}/TEST-${test.suite.class.name}.xml" message="&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot; ?&gt;&#10;&lt;testsuite name=&quot;${test.suite.name}&quot; type=&quot;${test-type}&quot; deploy=&quot;${deploy.name}&quot; hostname=&quot;${host.NAME}&quot; tests=&quot;1&quot; failure=&quot;0&quot;&gt;&#10;" />

		<available property="commlib.exist" file="${test.suite.fullname}/commlib.js" />
		<if>
			<isset property="commlib.exist" />
			<then>
				<echo message="${test.suite.fullname}/commlib is exist , then test will add it " />
				<sdbProperty name="commlib.option" value=",${test.suite.fullname}/commlib.js" />
			</then>	
			<else>
				<echo message="${test.suite.fullname}/commlib is not exist , then test will not add it" />
				<sdbProperty name="commlib.option" value="" />
			</else>
		</if>

		<foreach target="test-one-case" param="test.case.fullname">
			<fileset dir="${test.suite.fullname}" includes="${includes.case.patterns}" excludes="${excludes.case.patterns}" />
			<param name="testcases.path" value="${testcases.path}" />
			<param name="test-type" value="${test-type}" />
			<param name="test.suite.class.name" value="${test.suite.class.name}" />

			<param name="host.NAME" value="${host.NAME}" />
			<!-- <param name="parallel.num" value="${parallel.num}" /> -->
			<param name="commlib.option" value="${commlib.option}" />
		</foreach>

		<echo file="${reports.path}/TEST-${test.suite.class.name}.xml" message="&lt;/testsuite&gt;&#10;" append="true" />
	</target>



	<target name="test-one-case" depends="">
		<exec executable="${sdb.fullname}" timeout="300000" dir="" outputproperty="db.RG.info" resultproperty="ret.code.forShards">
			<arg line=" -s &quot; db = new Sdb('localhost',${coord.service.port});var rg = db.listReplicaGroups();println(rg);db.close(); &quot;" />
		</exec>
		<if>
			<not>
				<equals arg1="${ret.code.forShards}" arg2="0" />
			</not>
			<then>
				<sdbProperty name="db.RG.info" value="this env is standalone , host is ${test.machine.no2.basic}"/>
			</then>
		</if>
		<if>
			<equals arg1="${os.arch}" arg2="ppc64" />
			<then>
				<property name="URL.download.pre" value="http://192.168.30.150:8080" />
			</then>
			<else>
				<property name="URL.download.pre" value="http://192.168.30.184:8080" />
			</else>
		</if>

		<tstamp>
			<format property="testcase.begin.time" pattern="yyyy-MM-dd-hh.mm.ss" />
		</tstamp>

		<basename property="test.case.name" file="${test.case.fullname}" suffix=".js" />
		<exec executable="sed">
			<arg line="-i 's/^\xEF\xBB\xBF//g'  ${test.case.fullname}"/>
		</exec>
		<exec executable="sed">
			<arg line="-i 's/^\xEF\xBB\xBF//g'  ${testcases.path}/libs/func.js"/>
		</exec>
		<echo message="Now testcase ${test.case.name}.js begin with ${testcase.begin.time}"/>

		<if>
			<equals arg1="${test-type}" arg2="basic" />
			<then>
				<sdbprefix prefixName="CS_PRIX">
					<param name="${host.NAME}" value="${host.NAME}" />
					<param name="${test.case.name}" value="${test.case.name}" />
					<param name="${parallel.num}" value="${parallel.num}" />
				</sdbprefix>

				<sdbProperty name="testBeginStr" value="begin test,file is ${test.case.fullname}" />
				<sdbProperty name="testEndStr" value="over test,file is ${test.case.fullname}" />
				<exec executable="${sdb.fullname}" dir="" timeout="300000">
					<arg line=" -s &quot; db = new Sdb('localhost',${coord.service.port});db.msg('${testBeginStr}');db.close(); &quot;" />
				</exec>

				<property name="sdb.cmd.beforeTestcase" value=" -e &quot; var COORDSVCNAME='${coord.service.port}' ;&quot; -f &quot;${testcases.path}/libs/func.js ${commlib.option} ,${testcases.path}/libs/before_usecase.js&quot;" />
				<echo message="Exec:${sdb.fullname}${sdb.cmd.beforeTestcase}" />
				<exec executable="${sdb.fullname}" dir="" timeout="1200000" outputproperty="beforeTestcase.screen.output" resultproperty="beforeTestcase.ret.code">
					<arg line="${sdb.cmd.beforeTestcase}" />
				</exec>
				<echo message="${beforeTestcase.screen.output}" />

				<property name="sdb.cmd.argument" value=" -e &quot;var CSPREFIX='${CS_PRIX}';var COORDSVCNAME='${coord.service.port}';&quot; -f &quot;${testcases.path}/libs/func.js ${commlib.option},${test.case.fullname}&quot;" />
				<property name="command" value="${sdb.fullname}${sdb.cmd.argument}" />
				<echo message="Command:${command}" />
				<echo message="The testcase ${test.case.fullname} path on ${testcases.path}" />
				<exec executable="${sdb.fullname}" dir="" timeout="1200000" outputproperty="screen.output" resultproperty="ret.code" failonerror="false">
					<arg line="${sdb.cmd.argument}" />
				</exec>
				<echo message="${screen.output}" />
				
				<property name="sdb.cmd.afterTestcase" value=" -e &quot; var COORDSVCNAME='${coord.service.port}' ;&quot; -f &quot;${testcases.path}/libs/func.js ${commlib.option} ,${testcases.path}/libs/after_usecase.js&quot;" />
				<echo message="Exec:${sdb.fullname} ${sdb.cmd.afterTestcase}" />
				<exec executable="${sdb.fullname}" dir="" timeout="1200000" outputproperty="afterTestcase.screen.output" resultproperty="afterTestcase.ret.code">
					<arg line="${sdb.cmd.afterTestcase}" />
				</exec>
				<echo message="${afterTestcase.screen.output}" />

				<echo message="the result = ${ret.code} after exec ${test.case.fullname} testcase." />
				<exec executable="${sdb.fullname}" dir="" timeout="300000">
					<arg line=" -s &quot; db = new Sdb('localhost',${coord.service.port});db.msg('${testEndStr}');db.close(); &quot;" />
				</exec>
			</then>
			<else>
				<!-- samples testcases path -->
			</else>
		</if>

		<echo file="${reports.path}/TEST-${test.suite.class.name}.xml" append="true" message="&lt;testcase classname=&quot;${test.suite.class.name}&quot; name=&quot;${test.case.name}&quot; &gt;&#10;" />

		<if>
			<not>
				<equals arg1="${ret.code}" arg2="0" />
			</not>
			<then>
				<trycatch>
					<try>
						<if>
							<equals arg1="${to.copy.test.log}" arg2="true" />
							<then>
								<echo message="to.copy.test.log is true , then copy all test log to ${save.testLog.hostname}:${save.testLog.dir}"/>
								<tstamp>
									<format property="current.time.log" pattern="yyyy-MM-dd-hh.mm.ss" />
								</tstamp>

								<echo message="mkdir ${test.machine.deploy.path}/${host.NAME}-${parallel.num}-${current.time.log}" />
								<mkdir dir="${test.machine.deploy.path}/${host.NAME}-${parallel.num}-${current.time.log}" />

								<if>
									<not>
										<equals arg1="${ret.code.forShards}" arg2="0" />
									</not>
									<then>
									<!--statealong mode-->
										<sdbCopyAndTar fileHostName="LOCAL" saveHostName="${host.NAME}" diaglogPath="${install.path}/sequoiadb/database" savePath="${test.machine.deploy.path}/${host.NAME}-${parallel.num}-${current.time.log}" />
									</then>
									<else>
										<sdbCopyAndTar fileHostName="${test.machine.no1.basic}" saveHostName="${host.NAME}" diaglogPath="${install.path}/sequoiadb/database" savePath="${test.machine.deploy.path}/${host.NAME}-${parallel.num}-${current.time.log}" />
										<sdbCopyAndTar fileHostName="${test.machine.no2.basic}" saveHostName="${host.NAME}" diaglogPath="${install.path}/sequoiadb/database" savePath="${test.machine.deploy.path}/${host.NAME}-${parallel.num}-${current.time.log}" />
										<sdbCopyAndTar fileHostName="${test.machine.no3.basic}" saveHostName="${host.NAME}" diaglogPath="${install.path}/sequoiadb/database" savePath="${test.machine.deploy.path}/${host.NAME}-${parallel.num}-${current.time.log}" />
									</else>
								</if>

								<tar basedir="${test.machine.deploy.path}" destfile="${test.machine.deploy.path}/${host.NAME}-${parallel.num}-${current.time.log}.tar.gz" compression="gzip" includes="${host.NAME}-${parallel.num}-${current.time.log}/**" />
								<stafTools workHost="${host.NAME}" fileName="${test.machine.deploy.path}/${host.NAME}-${parallel.num}-${current.time.log}.tar.gz" workType="copy" toHost="${save.testLog.hostname}" workDir="${test.machine.deploy.path}" saveDir="${save.testLog.dir}" />
								<sdbProperty name="delete.diaglogFile" value="dir=$(find /opt/sequoiadb/database -name diaglog); for d in $dir ; do echo rm -f $d/* ; rm -f $d/* ; done" />
						
								<if>
									<not>
										<equals arg1="${ret.code.forShards}" arg2="0" />
									</not>
									<then>
									<!--standalone mode-->
										<stafTools workType="shell" workHost="LOCAL" workDir="${install.path}/sequoiadb/database" common="${delete.diaglogFile}" failonerror="false" />
									</then>
									<else>
										<stafTools workType="shell" workHost="${test.machine.no1.basic}" workDir="${install.path}/sequoiadb/database" common="${delete.diaglogFile}" failonerror="false" />
										<stafTools workType="shell" workHost="${test.machine.no2.basic}" workDir="${install.path}/sequoiadb/database" common="${delete.diaglogFile}" failonerror="false" />
										<stafTools workType="shell" workHost="${test.machine.no3.basic}" workDir="${install.path}/sequoiadb/database" common="${delete.diaglogFile}" failonerror="false" />
									</else>
								</if>

								<echo message="rm -f ${test.machine.deploy.path}/${host.NAME}-${parallel.num}-${current.time.log}" />
								<delete file="${test.machine.deploy.path}/${host.NAME}-${parallel.num}-${current.time.log}.tar.gz" failonerror="false" />
								<delete dir="${test.machine.deploy.path}/${host.NAME}-${parallel.num}-${current.time.log}" failonerror="false"/>
							</then>
						</if>
					</try>
					<catch>
						<echo message="copy log for download code fail" />
					</catch>
					<finally>
						<if>
							<not>
								<equals arg1="${allPrepare.ret.code}" arg2="0" />
							</not>
							<then>
								<echo file="${reports.path}/TEST-${test.suite.class.name}.xml" append="true" message="&lt;message error=&quot;${allPrepare.screen.output}&quot;/&gt;&#10;" />
							</then>
						</if>
						<if>
							<not>
								<equals arg1="${allClean.ret.code}" arg2="0" />
							</not>
							<then>
								<echo file="${reports.path}/TEST-${test.suite.class.name}.xml" append="true" message="&lt;message error=&quot;${allClean.screen.output}&quot;/&gt;&#10;" />
							</then>
						</if>
						<if>
							<not>
								<equals arg1="${beforeTestcase.ret.code}" arg2="0" />
							</not>
							<then>
								<echo file="${reports.path}/TEST-${test.suite.class.name}.xml" append="true" message="&lt;message error=&quot;${beforeTestcase.screen.output}&quot;/&gt;&#10;" />
							</then>
						</if>
						<if>
							<not>
								<equals arg1="${afterTestcase.ret.code}" arg2="0" />
							</not>
							<then>
								<echo file="${reports.path}/TEST-${test.suite.class.name}.xml" append="true" message="&lt;message error=&quot;${afterTestcase.screen.output}&quot;/&gt;&#10;" />		
							</then>
						</if>

						<tstamp>
							<format property="testcase.failure.end.time" pattern="yyyy-MM-dd-hh.mm.ss" />
						</tstamp>
						<sdbTime beginTime="${testcase.begin.time}" endTime="${testcase.failure.end.time}" output="testcase.failure.timeOutput" />
						<echo message="The result code is ${ret.code} and it's fail ..." />
						<echo message="The testcase ${test.case.name}.py end with ${testcase.failure.end.time} and cost ${testcase.failure.timeOutput} s in total..." />

						<echo file="${reports.path}/TEST-${test.suite.class.name}.xml" append="true" message="&lt;testcase name=&quot;${test.case.name}&quot; begin_time=&quot;${testcase.begin.time}&quot; end_time=&quot;${testcase.failure.end.time}&quot; time=&quot;${testcase.failure.timeOutput}&quot; path=&quot;${testcases.path}&quot; status=&quot;fail&quot;&gt;&#10;&lt;failure&gt;&#10;&lt;![CDATA[COMMAND= ${command}&#10;RETURNCODE=${ret.code}&#10;ERRORMESSAGE=&#10;${screen.output}&#10;TESTBEGINSTR=${testBeginStr}&#10;TESTENDSTR=${testEndStr}&#10;db.listReplicaGroups():&#10;${db.RG.info} &#10;download the test log :&#10;${URL.download.pre}/${host.NAME}-${parallel.num}-${current.time.log}.tar.gz]]&gt;&#10;&lt;/failure&gt;&#10;" />

						<sdbHaveMaster waitTime="10" propertyName="haveMaster" groupName="1" hostName="localhost" failonerror="false" />
						<echo message="${haveMaster}" />
						<if>
							<equals arg1="${haveMaster}" arg2="true"/>
							<then>
								<echo message="haveMaster is true"/>
								<sdbClearEnvCS hostName="localhost" port="50000" csprefix="${CS_PRIX}"/>
							</then>
							<else>
								<echo message="test is failed , CS_PRIX is ${CS_PRIX} , but when drop the cs , there are no catalog master node" />
							</else>
						</if>
					</finally>
				</trycatch>
			</then>
			<else>
				<tstamp>
					<format property="testcase.end.time" pattern="yyyy-MM-dd-hh.mm.ss" />
				</tstamp>
				<sdbTime beginTime="${testcase.begin.time}" endTime="${testcase.end.time}" output="testcase.timeOutput" />
				<echo message="The result code is ${ret.code} and it's succ ..." />
				<echo message="The testcase ${test.case.name}.py end with ${testcase.end.time} and cost ${testcase.timeOutput} s in total..." />

				<echo file="${reports.path}/TEST-${test.suite.class.name}.xml" append="true" message="&lt;testcase name=&quot;${test.case.name}&quot; begin_time=&quot;${testcase.begin.time}&quot; end_time=&quot;${testcase.end.time}&quot; time=&quot;${testcase.timeOutput}&quot; path=&quot;${testcases.path}&quot; status=&quot;succ&quot;&gt;&#10;" />
			</else>
		</if>

		<sdbHaveMaster waitTime="10" propertyName="haveMaster" groupName="1" hostName="localhost" failonerror="false" />
		<echo message="${haveMaster}" />
		<if>
			<equals arg1="${haveMaster}" arg2="true"/>
			<then>
				<echo message="haveMaster is true"/>
				<sdbClearEnvCS hostName="localhost" port="50000" csprefix="${CS_PRIX}"/>
			</then>
			<else>
				<echo message="test is failed , CS_PRIX is ${CS_PRIX} , but when drop the cs , there are no catalog master node" />
			</else>
		</if>

		<echo file="${reports.path}/TEST-${test.suite.class.name}.xml" append="true" message="&lt;/testcase&gt;&#10;" />
	</target>
</project>