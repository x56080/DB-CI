<!--@param ${install.package.fullname}: The path where place software package.-->
<!--@param ${deploy.file.fullname}: The file full path where deploy file name for test -->

<!--@param ${test.reports.path}: The path where test reports directory. -->
<!--@param ${deploy.case.fullname}:all kind of test environment. -->
<!--@param ${WORKSPACE}: where the working space is it-->
<!--@param ${DEPLOY_WILDCARD}: deploy config file wildcard. just for debug -->
<!--@param ${test.environment} : the control the test Environment -->
<!--@param ${ci.script.file.path} : where the ci.test.properties file is it -->

<!-- hostname:jenkins.master.ip -->
<!-- depends:deploy-test-all.xml -->
<!-- call:deploy-software-in-host.xml,deploy-test-in-host.xml,exec-basic-testcases.xml -->

<project name="H3-G1-D3-P2" default="H3-G1-D3-P2">
	<property name="properties.dir" value="${basedir}/../ci.test.properties" />
	<property file="${properties.dir}" />

	<target name="Deploy" depends="">
		<parallel>
			<ant antfile="deploy-software-in-host.xml" dir="." inheritAll="false">
				<property name="install.package.fullname" value="${install.package.fullname}" />
				<property name="deploy.host.name" value="${test_machine_no1}" />
				<property name="is.standalone.mode" value="false" />
				<property name="is.coord.master" value="true"/>
				<property name="catalog.addrs" value="${test_machine_no1}:30003,${test_machine_no2}:30003,${test_machine_no3}:30003" />
			</ant>
			<ant antfile="deploy-software-in-host.xml" dir="." inheritAll="false">
				<property name="install.package.fullname" value="${install.package.fullname}" />
				<property name="deploy.host.name" value="${test_machine_no2}" />
				<property name="is.standalone.mode" value="false" />
				<property name="is.coord.master" value="false"/>
				<property name="catalog.addrs" value="${test_machine_no1}:30003,${test_machine_no2}:30003,${test_machine_no3}:30003" />
			</ant>
			<ant antfile="deploy-software-in-host.xml" dir="." inheritAll="false">
				<property name="install.package.fullname" value="${install.package.fullname}" />
				<property name="deploy.host.name" value="${test_machine_no3}" />
				<property name="is.standalone.mode" value="false" />
				<property name="is.coord.master" value="false"/>
				<property name="catalog.addrs" value="${test_machine_no1}:30003,${test_machine_no2}:30003,${test_machine_no3}:30003" />
			</ant>
		</parallel>

    	<if>
		   	<equals arg1="${os.arch}" arg2="amd64" />
			<then>
				<property name="fap.value" value="fapmongo" />
			</then>
			<else>
				<property name="fap.value" value="" />
			</else>
		</if>
		<sdbdeploy host="${test_machine_no1}" coordport="${coord.service.port}">
			<catagroup name="cata1">
				<node host="${test_machine_no1}" baseport="${cata.service.port}" dbpath="${install.path}/sequoiadb/database/cata/${cata.service.port}">
					<confitem name="diaglevel" value="5" />
					<confitem name="transactionon" value="true" />
					<confitem name="sharingbreak" value="300000" />
					<confitem name="fap" value="${fap.value}" />
				</node>
				<node host="${test_machine_no2}" baseport="${cata.service.port}" dbpath="${install.path}/sequoiadb/database/cata/${cata.service.port}">
					<confitem name="diaglevel" value="5" />
					<confitem name="transactionon" value="true" />
					<confitem name="sharingbreak" value="300000" />
					<confitem name="fap" value="${fap.value}" />
				</node>
				<node host="${test_machine_no3}" baseport="${cata.service.port}" dbpath="${install.path}/sequoiadb/database/cata/${cata.service.port}">
					<confitem name="diaglevel" value="5" />
					<confitem name="transactionon" value="true" />
					<confitem name="sharingbreak" value="300000" />
					<confitem name="fap" value="${fap.value}" />
				</node>
			</catagroup>
		</sdbdeploy>

		<sdbdeploy host="${test_machine_no1}" coordport="${coord.service.port}">
			<datagroup name="group1">
				<node host="${test_machine_no1}" baseport="${data.service.port}" dbpath="${install.path}/sequoiadb/database/data/${data.service.port}">
					<confitem name="diaglevel" value="5" />
					<confitem name="transactionon" value="true" />
					<confitem name="sharingbreak" value="300000" />
					<confitem name="diagnum" value="80" />
					<confitem name="fap" value="${fap.value}" />
				</node>
				<node host="${test_machine_no2}" baseport="${data.service.port}" dbpath="${install.path}/sequoiadb/database/data/${data.service.port}">
					<confitem name="diaglevel" value="5" />
					<confitem name="transactionon" value="true" />
					<confitem name="sharingbreak" value="300000" />
					<confitem name="diagnum" value="80" />
					<confitem name="fap" value="${fap.value}" />
				</node>
				<node host="${test_machine_no3}" baseport="${data.service.port}" dbpath="${install.path}/sequoiadb/database/data/${data.service.port}">
					<confitem name="diaglevel" value="5" />
					<confitem name="transactionon" value="true" />
					<confitem name="sharingbreak" value="300000" />
					<confitem name="diagnum" value="80" />
					<confitem name="fap" value="${fap.value}" />
				</node>
			</datagroup>
		</sdbdeploy>
	</target>

	<target name="H3-G1-D3-P2" depends="">
		<hostinfo prefix="host" />
		<property name="test-type-list" value="basic" />
		<property name="copy.test.log" value="true"/>

		<parallel>
			<ant antfile="deploy-test-in-host.xml" dir="." inheritAll="false">
				<property name="deploy.file.fullname" value="${deploy.file.fullname}" />
				<property name="deploy.host.name" value="${test_machine_no1}" />
			</ant>
			<ant antfile="deploy-test-in-host.xml" dir="." inheritAll="false">
				<property name="deploy.file.fullname" value="${deploy.file.fullname}" />
				<property name="deploy.host.name" value="${test_machine_no2}" />
			</ant>
			<ant antfile="deploy-test-in-host.xml" dir="." inheritAll="false">
				<property name="deploy.file.fullname" value="${deploy.file.fullname}" />
				<property name="deploy.host.name" value="${test_machine_no3}" />
			</ant>
		</parallel>

		<tstamp>
			<format property="current.time.log" pattern="yyyy-MM-dd-hh.mm.ss_a" />
		</tstamp>
		<property name="test.log.path" value="${test.reports.path}/${test.environment}-${DEPLOY_WILDCARD}-${host.NAME}-${current.time.log}-${BUILD_NUMBER}" />
		<property name="test.log" value="${test.environment}-${DEPLOY_WILDCARD}-${host.NAME}-${current.time.log}-${BUILD_NUMBER}" />

		<parallel>
			<echo message="Begin to test basic tests under parallel env on a machine..." />
			<sdbtest host="${test_machine_no2}" testscript="${test.machine.deploy.path}/deploy/exec-basic-testcases.xml" remotereports="${testcase.path}/basic_testcases/reports" masterreports="${test.reports.path}" >
				<param name="basic.testcase.path" value="${testcase.path}/basic_testcases" />
				<param name="includes.dir.patterns" value="*" />
				<param name="excludes.dir.patterns" value="vote" />
				<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
				<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
				<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
				<param name="WORKSPACE" value="${WORKSPACE}" />
				<param name="ci.script.file.path" value="${ci.script.file.path}" />
				<param name="parallel.count" value="2" />

				<param name="test.log" value="${test.log}" />
			</sdbtest>
			<sdbtest host="${test_machine_no2}" testscript="${test.machine.deploy.path}/deploy/exec-basic-testcases.xml" remotereports="${testcase.path}/basic_testcases/reports" masterreports="${test.reports.path}" >
				<param name="basic.testcase.path" value="${testcase.path}/basic_testcases" />
				<param name="includes.dir.patterns" value="*" />
				<param name="excludes.dir.patterns" value="vote" />
				<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
				<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
				<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
				<param name="WORKSPACE" value="${WORKSPACE}" />
				<param name="ci.script.file.path" value="${ci.script.file.path}" />
				<param name="parallel.count" value="2" />
				<param name="test.log" value="${test.log}" />
			</sdbtest>
		</parallel>

		<trycatch>
			<try>
				<if>
					<equals arg1="${copy.test.log}" arg2="true" />
					<then>
						<echo message="The copy.test.log is true , then copy all test log to ${save.testLog.hostname}:${save.testLog.dir}"/>
						<tstamp>
							<format property="current.time.log" pattern="yyyy-MM-dd-hh.mm.ss a" />
						</tstamp>

						<property name="test.log.path" value="${test.reports.path}/${test.environment}-${DEPLOY_WILDCARD}-${host.NAME}-${current.time.log}-${BUILD_NUMBER}" />
						<echo message="Mkdir ${test.log.path}" />
						<mkdir dir="${test.log.path}" />

						<echo message="It's the cluster mod..." />
						<sdbCopyAndTar fileHostName="${test_machine_no1}" saveHostName="${host.NAME}" diaglogPath="${install.path}/sequoiadb/database" savePath="${test.log.path}" />
						<sdbCopyAndTar fileHostName="${test_machine_no2}" saveHostName="${host.NAME}" diaglogPath="${install.path}/sequoiadb/database" savePath="${test.log.path}" />
						<sdbCopyAndTar fileHostName="${test_machine_no3}" saveHostName="${host.NAME}" diaglogPath="${install.path}/sequoiadb/database" savePath="${test.log.path}" />

						<echo message="Tar ${test.log.path}.tar.gz..." />
						<tar basedir="${test.reports.path}" destfile="${test.log.path}.tar.gz" compression="gzip" includes="**/*" />

						<stafTools workType="copy" workHost="${host.NAME}" fileName="${test.log.path}.tar.gz"  toHost="${save.testLog.hostname}"  saveDir="${save.testLog.dir}" />
						<sdbProperty name="delete.diaglogFile" value="dir=$(find /opt/sequoiadb/database -name diaglog); for d in $dir ; do echo rm -f $d/* ; rm -f $d/* ; done" />

						<stafTools workType="shell" workHost="${test_machine_no1}" workDir="${install.path}/sequoiadb/database" common="${delete.diaglogFile}" failonerror="false" />
						<stafTools workType="shell" workHost="${test_machine_no2}" workDir="${install.path}/sequoiadb/database" common="${delete.diaglogFile}" failonerror="false" />
						<stafTools workType="shell" workHost="${test_machine_no3}" workDir="${install.path}/sequoiadb/database" common="${delete.diaglogFile}" failonerror="false" />

						<echo message="Delete ${test.log.path}.tar.gz and ${test.log.path} dir ..." />
						<delete file="${test.log.path}.tar.gz" failonerror="false" />
						<delete dir="${test.log.path}" failonerror="false"/>
					</then>
				</if>
			</try>
			<catch>
				<echo message="copy log for download code fail" />
			</catch>
			<finally>
			</finally>
		</trycatch>
	</target>
</project>
