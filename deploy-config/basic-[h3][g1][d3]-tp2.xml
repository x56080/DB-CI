<!--@param ${install.package.fullname}: The path where place software package.-->
<!--@param ${deploy.file.fullname}: The file full path where deploy file name for test -->

<!--@param ${test.reports.path}: The path where test reports directory. -->
<!--@param ${deploy.case.fullname}:all kind of test environment. -->

<!--@param ${DEPLOY_WILDCARD}: deploy config file wildcard. just for debug -->
<!--@param ${test.environment} : the control the test Environment -->
<!--@param ${ci.script.file.path} : where the ci.test.properties file is it -->

<!-- hostname:jenkins.master.ip -->
<!-- depends:deploy-test-all.xml -->
<!-- call:deploy-software-in-host.xml,deploy-test-in-host.xml,exec-basic-testcases.xml -->

<project default="H3-G1-D3-P2">
	<property name="properties.dir" value="${basedir}/../ci.test.properties" />
	<property file="${properties.dir}" />
	<property name="isTrunk" value="true" />
	<if>
	   	<equals arg1="${isTrunk}" arg2="true" />
		<then>
		    <property name="machineList" value="${trunk_machine}" />
		</then>
		<else>
		    <property name="machineList" value="${branch_machine}" />
		</else>
	</if>

	<target name="deploySoftwareInHost">
        <property name="addrTemp" value="tmp" />
        <for list="${machineList}" param="hostnames">
            <sequential>
                <var name="addrTemp" value="@{hostnames}:11803,${addrTemp}" />
           </sequential>
        </for>
        <basename property="catalog.addrs" file="${addrTemp}" suffix=",tmp" />
        <ant antfile="deploy-software-in-host.xml" dir="." inheritAll="false" output="${hostname}">
			<property name="install.package.fullname" value="${install.package.fullname}" />
			<property name="deploy.host.name" value="${hostname}" />
			<property name="is.standalone.mode" value="false" />
			<property name="is.coord.master" value="true"/>
			<property name="catalog.addrs" value="${catalog.addrs}" />
		</ant>
    </target>

	<target name="Deploy-Software" depends="">
		<if>
		   	<equals arg1="${os.arch}" arg2="amd64" />
			<then>
			    <property name="fap.value" value="fapmongo" />
			</then>
			<else>
			    <property name="fap.value" value="" />
			</else>
		</if>

		<for list="${machineList}" param="hostname">
            <sequential>
                <antcall target="deploySoftwareInHost" inheritAll="true" >
                    <param name="hostname" value="@{hostname}" />
                    <param name="machineList" value="${machineList}" />
                </antcall>
            </sequential>
        </for>

        <for list="${machineList}" param="hostname">
    		<sequential>
		    	<sdbdeploy host="${trunk_machine_control}" coordport="${coord.service.port}">
		    		<catagroup name="cata1">
        				<node host="@{hostname}" baseport="${cata.service.port}" dbpath="${install.path}/sequoiadb/database/cata/${cata.service.port}">
							<confitem name="diaglevel" value="5" />
							<confitem name="transactionon" value="true" />
							<confitem name="sharingbreak" value="30000" />
							<confitem name="fap" value="${fap.value}" />
							<confitem name="optimeout" value="60000" />
						</node>
		    		</catagroup>
		    	</sdbdeploy>
        	</sequential>
		</for>

		<for list="${machineList}" param="hostname">
    		<sequential>
				<sdbdeploy host="${trunk_machine_control}" coordport="${coord.service.port}">
					<datagroup name="group1">			
						<node host="@{hostname}" baseport="21100" dbpath="${install.path}/sequoiadb/database/data/21100">
							<confitem name="diaglevel" value="5" />
							<confitem name="transactionon" value="true" />
							<confitem name="sharingbreak" value="30000" />
							<confitem name="diagnum" value="80" />
							<confitem name="fap" value="${fap.value}" />
							<confitem name="optimeout" value="60000" />
						</node>
					</datagroup>
				</sdbdeploy>
			</sequential>
		</for>
	</target>

	<target name="H3-G1-D3-P2" depends="">
		<hostinfo prefix="host" />
		<property name="copy.test.log" value="true"/>

		<for list="${machineList}" param="hostname" parallel="true">
			<sequential>
				<ant antfile="deploy-test-in-host.xml" dir="." inheritAll="false">
					<property name="deploy.file.fullname" value="${deploy.file.fullname}" />
					<property name="deploy.host.name" value="@{hostname}" />
				</ant>
			</sequential>
		</for>

		<property name="test.log" value="${test.environment}-${OS_NAME}${BUILD_NUMBER}-[H3][G1][D3]" />
		<property name="test.log.path" value="${test.reports.path}/${test.log}" />

		<!-- <if>
		   	<equals arg1="${os.arch}" arg2="amd64" />
			<then>
				<parallel>
					<sdbtest host="${test_machine_no2}" testscript="${test.machine.deploy.path}/deploy/exec-basic-testcases.xml" remotereports="${testcase.path}/js_testcases/reports" masterreports="${test.reports.path}" >
						<param name="basic.testcase.path" value="${testcase.path}/js_testcases" />
						<param name="includes.dir.patterns" value="${js_list1},${js_list2},${js_list3}" />
						<param name="excludes.dir.patterns" value="vote" />
						<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
						<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
						<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
						<param name="test.log" value="${test.log}" />
						<param name="test-type-list" value="${test-type-list}" />
					</sdbtest>
					<sdbtest host="${test_machine_no3}" testscript="${test.machine.deploy.path}/deploy/exec-basic-testcases.xml" remotereports="${testcase.path}/js_testcases/reports" masterreports="${test.reports.path}" >
						<param name="basic.testcase.path" value="${testcase.path}/js_testcases" />
						<param name="includes.dir.patterns" value="${js_list1},${js_list2},${js_list3}" />
						<param name="excludes.dir.patterns" value="vote" />
						<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
						<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
						<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
						<param name="test.log" value="${test.log}" />
						<param name="test-type-list" value="${test-type-list}" />
					</sdbtest>
				</parallel>
				<parallel>
					<sdbtest host="${test_machine_no5}" testscript="${test.machine.deploy.path}/deploy/exec-basic-testcases.xml" remotereports="${testcase.path}/js_testcases/reports" masterreports="${test.reports.path}" >
						<param name="basic.testcase.path" value="${testcase.path}/js_testcases" />
						<param name="includes.dir.patterns" value="${js_list4},${js_list5},${js_list6}" />
						<param name="excludes.dir.patterns" value="vote" />
						<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
						<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
						<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
						<param name="test.log" value="${test.log}" />
						<param name="test-type-list" value="${test-type-list}" />	
					</sdbtest>
					<sdbtest host="${test_machine_no6}" testscript="${test.machine.deploy.path}/deploy/exec-basic-testcases.xml" remotereports="${testcase.path}/js_testcases/reports" masterreports="${test.reports.path}" >
						<param name="basic.testcase.path" value="${testcase.path}/js_testcases" />
						<param name="includes.dir.patterns" value="${js_list4},${js_list5},${js_list6}" />
						<param name="excludes.dir.patterns" value="vote" />
						<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
						<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
						<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
						<param name="test.log" value="${test.log}" />
						<param name="test-type-list" value="${test-type-list}" />
					</sdbtest>
				</parallel>
			</then>
			<else>
				<parallel>
					<sdbtest host="${test_machine_no2}" testscript="${test.machine.deploy.path}/deploy/exec-basic-testcases.xml" remotereports="${testcase.path}/js_testcases/reports" masterreports="${test.reports.path}" >
						<param name="basic.testcase.path" value="${testcase.path}/js_testcases" />
						<param name="includes.dir.patterns" value="${js_list1},${js_list2},${js_list3}" />
						<param name="excludes.dir.patterns" value="vote" />
						<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
						<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
						<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
						<param name="test.log" value="${test.log}" />
						<param name="test-type-list" value="${test-type-list}" />
					</sdbtest>
					<sdbtest host="${test_machine_no3}" testscript="${test.machine.deploy.path}/deploy/exec-basic-testcases.xml" remotereports="${testcase.path}/js_testcases/reports" masterreports="${test.reports.path}" >
						<param name="basic.testcase.path" value="${testcase.path}/js_testcases" />
						<param name="includes.dir.patterns" value="${js_list1},${js_list2},${js_list3}" />
						<param name="excludes.dir.patterns" value="vote" />
						<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
						<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
						<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
						<param name="test.log" value="${test.log}" />
						<param name="test-type-list" value="${test-type-list}" />
					</sdbtest>
				</parallel>
			</else>
		</if> -->	


		<!-- <property name="test-type" value="js" />
		<for list="${test-type}" param="Test-Type">
			<sequential>
				<ant antfile="${WORKSPACE}/sequoiadb/misc/ci/test-type.xml" dir="." inheritAll="false">
					<property name="deploy.case.fullname" value="${deploy.case.fullname}" />
					<property name="test.reports.path" value="${test.reports.path}" />
					<property name="Test-Type" value="@{Test-Type}" />
					<property name="parallel.flag" value="parallel" />
					<property name="WORKSPACE" value="${WORKSPACE}" />
					<property name="ci.script.file.path" value="${ci.script.file.path}" />
					<property name="parallel.count" value="1" />
					<property name="test.log" value="${test.log}" />
					<property name="test-type-list" value="${test-type-list}" />
					<property name="BUILD_NUMBER" value="${BUILD_NUMBER}" />
				</ant>
			</sequential>
		</for>	 -->

		<for list="driver-java" param="Test-Type">
			<sequential>
				<ant antfile="${WORKSPACE}/sequoiadb/misc/ci/test-type.xml" dir="." inheritAll="false">
					<property name="test.host.NAME" value="${trunk_machine_control}" />
					<property name="deploy.case.fullname" value="${deploy.case.fullname}" />
					<property name="test.reports.path" value="${test.reports.path}" />
					<property name="Test-Type" value="@{Test-Type}" />
					<property name="parallel.flag" value="parallel" />
					<property name="WORKSPACE" value="${WORKSPACE}" />
					<property name="ci.script.file.path" value="${ci.script.file.path}" />
					<property name="parallel.count" value="1" />
					<property name="test.log" value="${test.log}" />
					<property name="test-type-list" value="${test-type-list}" />
					<property name="BUILD_NUMBER" value="${BUILD_NUMBER}" />
				</ant>
			</sequential>
		</for>

		<trycatch>
			<try>
				<if>
					<equals arg1="${copy.test.log}" arg2="true" />
					<then>
						<mkdir dir="${test.log.path}" />

						<for list="${machineList}" param="hostname" parallel="true">
							<sequential>
								<sdbCopyAndTar fileHostName="@{hostname}" saveHostName="${host.NAME}" diaglogPath="${install.path}/sequoiadb/database" savePath="${test.log.path}" />
							</sequential>
						</for>

						<tar basedir="${test.reports.path}" destfile="${test.log.path}.tar.gz" compression="gzip" includes="**/*" />
						<stafTools workType="copy" workHost="${host.NAME}" fileName="${test.log.path}.tar.gz"  toHost="${save.testLog.hostname}"  saveDir="${save.testLog.dir}" failonerror="true"/>						
					</then>
				</if>
			</try>
			<catch>
				<echo message="Copy log for download code fail" />
			</catch>
			<finally>
				<delete file="${test.log.path}.tar.gz" failonerror="false" />
				<delete dir="${test.log.path}" failonerror="false"/>
			</finally>
		</trycatch>
	</target>
</project>
