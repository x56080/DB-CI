		Ant脚本编码规范
修订记录2015-11-22 Ting YUInit初稿

变量名
	用下划线分隔，如package_dir。Ant标准中变量是以点号分隔，如package.dir，但是为了在Notepad++等编辑器中双击，便可以选中变量，且可以高亮所有，改用下划线分隔。
	为了区分变量是从jenkins界面上传入的，还是代码中的变量，代码变量统一使用小写，jenkins界面变量全部大写。
target名
	用下划线分隔，如ready_package。原因同变量名。
空白行
	合理使用空白行，使代码结构更加清晰。如target与target之间使用空白行分隔，复杂逻辑为了更加清晰也可采用空白行分隔。
缩进
	缩进统一为3个空格，与数据库代码的风格保持一致。由于不同的编辑器的tab键长度不同，所以请不要使用tab键，请设置编辑器的tab键为3个空格。
注释
	恰当地使用注释。注释格式为<!—copy package to remote host -->
	不要滥用注释。
扩展包
	使用扩展包ant-contrib-1.0b3.jar，支持if else语句、for循环、switch语句等。
	使用扩展包STAFAnt.jar，ant中调用staf的方式为：
	<staf  location="@{testhost}" service="FS"
          request="DELETE ENTRY ${INSTALL_DIR} RECURSE CONFIRM"
          throwBuildException=""/>
需要安装staf工具（安装方式为full），并配置环境变量才能使用<staf>标签。
export CLASSPATH="/usr/local/staf/lib/STAFAnt.jar:${CLASSPATH}"
使用宏
	ant中的宏<macrodef>相当于其他语言中的函数。当某些代码执行次数较多时，可以提取成宏，多次调用。或者代码逻辑较复杂时，也可以提取出宏而使逻辑更加清晰。
	当然macrodef能做的事情，target也能做到。但是从执行效率上看，macrodef的执行效率高于target。
	当jenkins运行ant脚本时，在控制台左侧会有各个target的锚点。点击锚点可以跳转到相应target的输出信息位置。而macrodef不会生成锚点。
target之间的调用关系
	推荐使用main target作为一个project的入口，在main target中依次调用其他target。如：
<project default="main" >  
   ……
   <target name="main">
      <antcall target="uninstall"/>
      <antcallback target="ready_package" return="package_name"/> 
      <antcall target="install"/>
      <antcall target="deploy"/>    
   </target>
   ……
</project>
	ant标准的做法是使用depend，如
<project default="main" >  
   ……
   <target name="uninstall" depends=" ">
   </target>
   ……
   <target name="install" depends="uninstall">
   </target>
   ……
   <target name="deploy" depends="install">
   </target>
   ……
   <target name="clean" depends="deploy">
   </target>
   ……
</project>
	但是depand方式有个坏处，target之间的调用关系不够清晰明了，调试脚本时，如只想执行install和clean时还需要多处depend内容。main方式时，只需要在main中注释相应的target即可。

